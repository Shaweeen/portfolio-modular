// SheetDB API Configuration
const sheetDBConfig = {
  apiKey: 'YOUR_SHEETDB_API_KEY',
  sheetName: 'YOUR_SHEET_NAME'
};

// Navbar Titles
const navbarTitles = {
  home: 'Home',
  about: 'About Me',
  projects: 'Projects',
  contact: 'Contact'
};

// Card Customization
const cardCustomization = {
  backgroundColor: '#ffffff',
  textColor: '#333333',
  borderRadius: '8px'
};

// Recent Updates Configuration
const recentUpdates = {
  updatesLimit: 5,
  showDate: true
};

export { sheetDBConfig, navbarTitles, cardCustomization, recentUpdates };